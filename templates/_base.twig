<!DOCTYPE>
<html>
<head>
    <title>{% block title %}FreeFeed{% endblock %}</title>
    <link href='//fonts.googleapis.com/css?family=Open+Sans:300italic,400italic,600italic,700italic,400,300,700,600&subset=latin,cyrillic' rel='stylesheet' type='text/css'>
    <link rel="stylesheet" type="text/css" href="/style/main.css"/>
</head>
<body>

<div class="section section_white">
    <div class="section__wrapper">
        <div class="header">
            <div class="header__site-title">
                <h1 class="site-title">
                    <a class="site-title__link" href="/">FreeFeed.net</a>
                </h1>
            </div>

            <div class="header__navigation">
                <div class="login-logout">
                    {% if app.request.attributes.get('_logged_in') %}
                        <form action="{{ path('logout_submit') }}" method="post">
                            <button type="submit">Выход</button>
                        </form>
                    {% else %}
                        <a href="{{ path('login') }}">Вход</a>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</div>

{% block everything %}{% endblock %}

</body>
</html>
